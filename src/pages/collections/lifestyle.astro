---
import Base from '../../layouts/Base.astro';
import { anonClient } from '../../lib/supabase';
import { safeHtml } from '../../lib/sanitize';

const sb = anonClient();
let category: any = null;
let products: any[] = [];

if (sb) {
  const [catRes, prodRes] = await Promise.all([
    sb.from('categories').select('*').eq('slug', 'lifestyle').maybeSingle(),
    sb.from('products').select('*').eq('product_type', 'Perfume'),
  ]);
  category = catRes.data;
  products = prodRes.data ?? [];
}

const siteUrl = import.meta.env.PUBLIC_SITE_URL || 'https://harmony-threads-xk7p.vercel.app';

const collectionSchema = {
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  "name": "Lifestyle Goods | Harmony Threads",
  "url": `${siteUrl}/collections/lifestyle`,
  "mainEntity": {
    "@type": "ItemList",
    "itemListElement": products.map((p, i) => ({
      "@type": "ListItem",
      "position": i + 1,
      "url": `${siteUrl}/product/${p.slug}`,
      "name": p.name
    }))
  }
};

const breadcrumb = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    { "@type": "ListItem", "position": 1, "name": "Home", "item": siteUrl },
    { "@type": "ListItem", "position": 2, "name": "Shop", "item": `${siteUrl}/shop` },
    { "@type": "ListItem", "position": 3, "name": "Lifestyle", "item": `${siteUrl}/collections/lifestyle` }
  ]
};
---

<Base
  title="Lifestyle Goods — Music-Inspired | Harmony Threads"
  description="Premium lifestyle goods for music lovers. Harmony Threads signature perfume and curated goods for rock fans who carry the music with them everywhere."
  structuredData={[collectionSchema, breadcrumb]}
  tags={['music lifestyle', 'fragrance', 'perfume', 'gift ideas music lover']}
>
  <div class="pt-32 pb-20 max-w-7xl mx-auto px-6">

    <nav class="flex items-center gap-2 text-xs text-muted mb-10" style="font-family:'Manrope',system-ui,sans-serif;">
      <a href="/" class="hover:text-cream transition-colors">Home</a>
      <span>/</span>
      <a href="/shop" class="hover:text-cream transition-colors">Shop</a>
      <span>/</span>
      <span class="text-cream">Lifestyle</span>
    </nav>

    <div class="grid lg:grid-cols-3 gap-16 mb-16">
      <div class="lg:col-span-2">
        <p class="text-gold text-xs font-semibold tracking-widest uppercase mb-3" style="font-family:'Manrope',system-ui,sans-serif;">Collection</p>
        <h1 class="text-5xl font-semibold text-cream leading-tight mb-6" style="font-family:'Playfair Display',Georgia,serif;">
          Lifestyle
        </h1>

        {category?.body_html ? (
          <div class="text-muted leading-relaxed space-y-3 [&_p]:mb-3 [&_strong]:text-cream"
            style="font-family:'Manrope',system-ui,sans-serif;"
            set:html={safeHtml(category.body_html)} />
        ) : (
          <div class="text-muted leading-relaxed" style="font-family:'Manrope',system-ui,sans-serif;">
            <p class="mb-4">Beyond the tees, Harmony Threads curates lifestyle goods that carry the same spirit as the music that inspired them. Our signature perfume is formulated for those who want to carry the mood of a great record with them through the day — bold, lasting, and unmistakably intentional.</p>
            <p>Each lifestyle piece makes a thoughtful gift for the music lover in your life. Or a quiet upgrade to your own everyday carry. Premium quality, curated with the same care we put into every graphic design.</p>
          </div>
        )}
      </div>
      <div>
        <div class="bg-charcoal-2 rounded-sm p-6 border border-white/10">
          <p class="text-xs font-semibold text-gold tracking-widest uppercase mb-4" style="font-family:'Manrope',system-ui,sans-serif;">Why lifestyle</p>
          <ul class="space-y-3 text-sm text-muted" style="font-family:'Manrope',system-ui,sans-serif;">
            <li>🎁 Perfect for gifts</li>
            <li>✨ Premium quality materials</li>
            <li>🎵 Music-inspired curation</li>
            <li>📦 Gift-ready packaging</li>
            <li>🔄 30-day return policy</li>
          </ul>
          <a href="/shop" class="block mt-6 text-sm text-gold hover:text-gold-light transition-colors" style="font-family:'Manrope',system-ui,sans-serif;">← Back to all products</a>
        </div>
      </div>
    </div>

    {products.length === 0 ? (
      <p class="text-muted text-center py-16" style="font-family:'Manrope',system-ui,sans-serif;">Products loading…</p>
    ) : (
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
        {products.map(p => {
          const price = `$${(p.price_pence / 100).toFixed(2)}`;
          const compareAt = p.compare_at_pence ? `$${(p.compare_at_pence / 100).toFixed(2)}` : null;
          return (
            <article class="group">
              <a href={`/product/${p.slug}`} class="block">
                <div class="overflow-hidden rounded-sm aspect-[3/4] bg-charcoal-2 mb-5">
                  {p.image_url ? (
                    <img src={p.image_url} alt={`${p.name} — Harmony Threads`}
                      class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" loading="lazy"/>
                  ) : (
                    <div class="w-full h-full flex items-center justify-center text-4xl">✨</div>
                  )}
                </div>
                <h2 class="text-cream font-medium mb-1" style="font-family:'Playfair Display',Georgia,serif; font-size:1.1rem;">{p.name}</h2>
                <div class="flex items-center gap-2 mt-1">
                  <span class="text-cream font-semibold" style="font-family:'Manrope',system-ui,sans-serif;">{price}</span>
                  {compareAt && <span class="text-muted text-sm line-through" style="font-family:'Manrope',system-ui,sans-serif;">{compareAt}</span>}
                </div>
              </a>
            </article>
          );
        })}
      </div>
    )}

    <div class="mt-16 pt-10 border-t border-white/10 flex flex-wrap gap-6">
      <a href="/collections/graphic-tees" class="text-sm text-gold hover:text-gold-light transition-colors" style="font-family:'Manrope',system-ui,sans-serif;">Graphic tees →</a>
      <a href="/collections/digital-downloads" class="text-sm text-gold hover:text-gold-light transition-colors" style="font-family:'Manrope',system-ui,sans-serif;">Digital downloads →</a>
      <a href="/contact" class="text-sm text-gold hover:text-gold-light transition-colors" style="font-family:'Manrope',system-ui,sans-serif;">Contact us →</a>
    </div>
  </div>
</Base>
